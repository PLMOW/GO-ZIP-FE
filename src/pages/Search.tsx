import styled from 'styled-components';

const Search = () => {
  return <Wrapper>Search</Wrapper>;
};

export default Search;

const Wrapper = styled.div.attrs({ id: 'search' })`
  width: 100vw;
  height: 100vh;
  background: teal;
`;
